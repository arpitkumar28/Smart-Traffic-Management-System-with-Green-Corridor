"use client";
import { useEffect,useState } from "react";
import { MapPinned, RefreshCw } from "lucide-react";
import { Shell } from "@/components/Shell";
import { MetricCard,Meter,PageHeader,Panel,StatusBadge } from "@/components/operations";
import { fetchSignals,type Signal } from "@/lib/api";
const fallback:Signal[]=[{id:"J01",name:"Malviya Nagar",status:"green",traffic_load:42,lat:0,lng:0},{id:"J02",name:"Panch Batti",status:"green",traffic_load:18,lat:0,lng:0},{id:"J03",name:"Ajmer Road",status:"yellow",traffic_load:82,lat:0,lng:0},{id:"J04",name:"Raja Park",status:"green",traffic_load:35,lat:0,lng:0}];
export default function TrafficNetworkPage(){const[signals,setSignals]=useState(fallback);const[live,setLive]=useState(false);useEffect(()=>{fetchSignals().then(data=>{if(data.length){setSignals(data);setLive(true)}}).catch(()=>undefined)},[]);const average=Math.round(signals.reduce((a,b)=>a+b.traffic_load,0)/signals.length);return <Shell><main className="ops-page"><PageHeader index="06" title="Traffic network" subtitle="Road and intersection monitoring across the connected city network" actions={<button className="ops-button" onClick={()=>window.location.reload()}><RefreshCw size={13}/>Refresh</button>}/><div className="ops-grid"><MetricCard label="Monitored intersections" value={signals.length} note={live?"Live signal API":"Simulation data"}/><MetricCard label="Network load" value={`${average}%`} tone={average>70?"amber":"green"} note="Average reported load"/><MetricCard label="High-load locations" value={signals.filter(s=>s.traffic_load>75).length} tone="amber" note="Needs operator review"/><MetricCard label="Network state" value="Adaptive" tone="plain" note="Decision Engine coordination"/></div><div className="ops-layout"><Panel title="Intersection status" subtitle="Live signal response when backend connectivity is available" className="overflow"><table className="ops-table"><thead><tr><th>Junction</th><th>Road</th><th>Load</th><th>Signal</th></tr></thead><tbody>{signals.map(s=><tr key={s.id}><td><strong>{s.id}</strong></td><td>{s.name}</td><td><div style={{minWidth:130}}><Meter value={s.traffic_load} tone={s.traffic_load>75?"red":s.traffic_load>55?"amber":"green"}/><span className="demo-label">{Math.round(s.traffic_load)}% traffic load</span></div></td><td><StatusBadge tone={s.status==="red"?"red":s.status==="yellow"?"amber":"green"}>{s.status}</StatusBadge></td></tr>)}</tbody></table></Panel><Panel title="Network view" subtitle="Intersection topology"><div className="network-graphic"><svg viewBox="0 0 160 170"><path d="M25 25L70 10L132 25L110 65L145 95L96 120L45 146L20 100L48 74Z"/><path d="M25 25L48 74L20 100M70 10L110 65L96 120M132 25L145 95M48 74L110 65M20 100L45 146L96 120M110 65L145 95"/>{[[25,25],[70,10],[132,25],[110,65],[145,95],[96,120],[45,146],[20,100],[48,74]].map(([cx,cy],i)=><circle key={i} cx={cx} cy={cy} r="6" className={i===3?"amber":"green"}/>)}</svg></div><div className="ops-callout"><b><MapPinned size={13}/> Connected view.</b> Load values are sourced from the existing signals endpoint when configured. Map geometry is an illustrative network view.</div></Panel></div></main></Shell>}
